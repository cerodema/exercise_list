+----------------------------------------------------
+ // what are strings on cpp, what is char * string
+  std::string myString = "I want ice cream\n";
+  std::cout<<myString;
+  
+     /*char *myStringPointer = myString.c_str("Die or bet");
+     std::cout<<*myStringPointer<<std::endl;*/
+  
+  char foo[20];
+  
+  foo[0] = 1;
+------------------------------------------------------
+#define SIZE 5
+
+using std::string;
+int main(){
+ string str[SIZE];
+        for(unsigned int i = 0; i < SIZE; i++)
+            std::getline(std::cin, str[i]);
+      
+        for(unsigned int i = 0; i < SIZE; i++)
+            std::cout<<str[i]<<" ";
+  
+return 0;
+}
+----------------------------------------------------------
+string prob[10];
+    prob[0] = "I would like to be certain";
+    prob[1] = "Legal, loss risk or scam";
+    prob[2] = "Uncertainty that it might not work at all";
+    
+    for(unsigned int i = 0; i<10; i++){
+        std::cout<<prob[i]<<std::endl;    
+    }
